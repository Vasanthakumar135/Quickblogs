# Project Rules

## Styling & ClassNames
- Never use `clsx` or `classnames` functions.
- Always use standard Tailwind CSS strings for `className` attributes (e.g., `className="px-4 py-2 bg-blue-500"`).
